# AI_Flashcard_Quiz
Developed a desktop application that auto-generates multiple-choice quizzes from uploaded documents (PDF, DOCX, PPTX, TXT) using POS tagging to extract key terms and WordNet to generate context-aware distractors. Built an interactive flashcard-style quiz mode with live scoring and PDF export of generated question sets.
